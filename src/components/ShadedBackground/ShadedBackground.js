import React from 'react';

import styles from './ShadedBackground.module.css';

const ShadedBackground = () => {
  return (
    <div className={styles.ShadedBackground} />
  );
};

export default ShadedBackground;
